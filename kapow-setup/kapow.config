# Enforce WPCS checks via Git pre-commit hooks?
skeleton_precommit_wpcs=true
# Use a front-end framework ? (foundation|bourbon|bootstrap)
sass_framework="noframework"
# Use the Normalize CSS reset?
sass_normalize=true
# Lint PHP, SCSS and Javascript?
grunt_linting=false
